var charabanc = {};

charabanc.register = function(action, service, done) {
  return done();
};

charabanc.unregister = function(action, service, done) {
  return done();
};

charabanc.send = function(action, data, done) {
  return done();
};

module.exports = charabanc;
